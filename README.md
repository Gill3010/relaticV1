# Relatic V1

Sistema de certificados y documentos verificables.

## Instalación

1. Copia `api/config.local.php.example` a `api/config.local.php`
2. Edita `api/config.local.php` y completa con tus credenciales reales (base de datos, SMTP, API key)
3. **Nunca** subas `api/config.local.php` a Git

## Requisitos

- PHP 7.4+
- MySQL
- Composer (`composer install`)
